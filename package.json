{
  "name": "secure-serverless-security-platform",
  "version": "0.1.0",
  "private": true,
  "description": "npm workspace root. Exists so that every CDK app and the shared Aspects package resolve to exactly ONE copy of aws-cdk-lib and constructs.",
  "workspaces": [
    "platform/lib/cdk-security",
    "infra/cdk",
    "platform/11-serverless"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "synth": "npm run synth --workspaces --if-present"
  }
}
