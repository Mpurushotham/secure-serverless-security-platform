{
  "name": "@ssp/golden-path-api",
  "version": "0.1.0",
  "private": true,
  "description": "Golden-path serverless API — the reference every new service starts from",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "jest --selectProjects unit invariants",
    "test:security": "jest --selectProjects security",
    "test:all": "jest",
    "synth": "cdk synth --quiet"
  },
  "dependencies": {
    "@ssp/cdk-security": "*",
    "aws-cdk-lib": "^2.174.0",
    "cdk-nag": "^3.0.2",
    "constructs": "^10.4.2"
  },
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "@types/aws-lambda": "^8.10.145",
    "aws-sdk-client-mock": "^4.1.0",
    "aws-sdk-client-mock-jest": "^4.1.0"
  }
}
